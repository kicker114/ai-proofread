# 2026-08-10

- 复测3（task jiljwH, 01:48）`proofread max` 全新目录 /tmp/moon_test3：DeepSeek 已恢复，4 块全部首次尝试成功，失败=0 空响应=0 JSON无效=0 墙钟=14.66s EXIT=0。对比 08-09 两轮（失败 2~3 块、trickle/空响应）——证实前两轮高失败率纯为 DeepSeek 高负载服务端行为，与 WorkBuddy 适配/审校逻辑无关。用户已确认此判断。
- 管线在 API 健康与不健康时均优雅：挂死被 worker 内 asyncio.wait_for 看门狗拦下、空响应被标 failed 可续跑（CC 已修，commit 0e60bd6/b9492b4）。
- 旁注：本轮 7 条发现中 5 条"被跳过/未定位"（review_prose_skipped.json），属 finding 定位/句子对齐环节问题，与 API 无关，待另查。

## 全本审校交付（子 agent 跑 ai-proofread 管线）
- 目标：/Users/kicker114/Downloads/月球/译文_审校批注.pdf（256 页，有文字层，82MB）。用户要求子 agent 用 ai-proofread 管线审校并字符级高亮批注到 PDF。
- 流程：pdf2md 抽取(36万字符, manifest 256 units) → proofread max（1480 块，跑 3 次 checkpoint 续跑，失败=0，空响应=0 超时=0）→ annotate --dry-run 门禁 → 写新 PDF。
- 交付：译文_审校批注_修订高亮.pdf（256 页，82.75MB）；源 SHA 前后一致 571e2f20…9dff3，原文件未改；备份 译文_审校批注_备份.pdf 已建。
- 统计：发现 798（LLM788+TGSCC10）；写入高亮 380（723 char-quad，跨 166 页 p7–p256）；仅预览未写 270（fragment/fuzzy 降级，未开 flag）；跳过 101；歧义拒写 47。fitz 复核：Highlight=532 Text=152，作者 AI审校 新=380，原有 304 批注保留。
- 未处理：20 页无文字层（插图/章扉，非扫描无需 OCR）；204 页降级 raw text；677 条 chunk 内定位失败丢弃（moon.review_skipped.json，已知对齐问题）。
- 未验证：逐页视觉核对、联网事实核验（pipeline 模式无 evidence，全标待核）、270 条 fragment/fuzzy 未人工逐条审阅。
- 安全：严格遵循 AGENTS.md 原文件只读、写新文件、dry-run 门禁、SHA 核对。

## 整理 moon.review_skipped.json → CSV
- 677 条被丢弃发现，结构：{reason, detail, original, corrected, ratio}；顶层 dict 含 schema/source_sha256/generated_at/count/dropped。
- 分类：no_op_change 502（原文=修订,正常跳过）、match_below_threshold 172（匹配率<60 疑似 LLM 改写）、match_no_location 3（无法定位）。其中原文≠修订实质改动 186 条。
- 产出：/tmp/moon_full/moon.review_skipped.csv（677 行明细：序号/原因/匹配率/说明/原文/建议修订/是否实质改动）+ moon.review_skipped_summary.csv（3 行分类汇总）。
- 衍生：moon.review_skipped_changes.csv（186 条原文≠修订的实质改动，含字符级 diff 聚焦列「改动差异(原文↔修订)」。分布 匹配率过低172 + 无实质变化12(空白/标点差) + 无法定位2）。
